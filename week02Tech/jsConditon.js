// condition
if (condition) {
  // code execute
}

if (condition === true) {
  // the code in this block will execute
} else if (condition === false) {
  // the code in this block will execute
} else {
  // none of those condition is true
  // the code in this block will execute
}
